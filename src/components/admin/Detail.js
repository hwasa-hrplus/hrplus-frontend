import React from 'react';
//이거는 예시 화면(잘 넘어가는지 확인하고 지워주세오)

function Detail({ history, location, match }) {
    console.log('Detail!!!!');

    
    return (
        <div>
            <h2>여기는 Detail</h2>

        </div>
    );
}

export default Detail;